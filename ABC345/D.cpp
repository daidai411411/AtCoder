#include <stdlib.h>

#include <algorithm>
#include <cmath>
#include <iostream>
#include <list>
#include <numeric>
#include <string>
#include <vector>

using namespace std;

int main() {
	int n, h, w;
	cin >> n >> h >> w;
	vector<vector<bool>> space(h, vector<bool>(w, false));
	
	vector<int> a(n), b(n);
	for (int i = 0; i < n; i++){
		cin >> a[i] >> b[i];
	}

	vector<int> r(n), c(n);

	bool 

	

	for (int )

	return 0;
}
